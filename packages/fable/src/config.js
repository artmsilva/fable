/**
 * Project configuration
 * Change PROJECT_NAME here to rename the entire project
 */
export const PROJECT_NAME = "fable";

// Derived constants
export const THEME_STORAGE_KEY = `${PROJECT_NAME}-theme`;
