# Thread: <concise name>

| Field | Value |
| ----- | ----- |
| **Status** | `Done` |
| **Owner** | `codex` |
| **Start Date** | `2025-11-30` |
| **Last Update** | `2025-11-30 00:18 EST` |
| **Related Work** | `src/404.html`, `src/index.html`, `src/router.js` |
| **Links** | `n/a` |

## Objective

Ensure GitHub Pages deep links (e.g., `/components/...`, `/docs/...`) load the SPA instead of the GitHub 404. Provide a client-side redirect fallback that preserves the intended path and base path.

## Deliverables

- [x] Add redirect fallback in `src/404.html` to send users back to the app while preserving path.
- [x] Teach `src/index.html` to restore the intended path before router init.
- [x] Document testing outcome.

Update this checklist as tasks evolve.

## Timeline

- `2025-11-30 00:06 EST` &mdash; **Kickoff**: Scoped 404 redirect + index restore approach for GitHub Pages deep links.
- `2025-11-30 00:13 EST` &mdash; **Dev**: 404 now captures path + redirects to repo root; index restores saved path before router initializes.
- `2025-11-30 00:15 EST` &mdash; **Testing**: Not run (docs/HTML change only). Needs verification on GH Pages by opening a deep link (e.g., `/components/button/default`).
- `2025-11-30 00:18 EST` &mdash; **Outcome**: 404 redirect + index restore shipped; awaiting remote GH Pages confirmation.

## Current Risks / Blockers

- None yet.

## Hand-off Notes

- Summarize what’s next, where to resume in the codebase, and any context not obvious from commit history.

## Outcome (fill in when Done)

- 404 fallback now captures the requested path and redirects to root; index restores it before routing, preventing deep-link 404s on GH Pages.
